import React from 'react'
import { Layout } from '../components'

type Props = {}

export default function Merch(props: Props) {
  const {} = props

  return (
    <Layout>
      <span className="text-2xl font-bold">Buy the record:</span>
    </Layout>
  )
}
